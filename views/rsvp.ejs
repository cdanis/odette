<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/static/styles.css" />
  <title>RSVP - <%= attendee.event_title %></title>
</head>
<body>
  <div class="container">
    <h1><%= attendee.event_title %></h1>
    <p class="date"><%= new Date(attendee.event_date).toLocaleDateString() %></p> <!-- Changed attendee.date to attendee.event_date -->
    <% if (attendee.event_desc) { %> <!-- Changed attendee.description to attendee.event_desc -->
      <p class="description"><%= attendee.event_desc %></p>
    <% } %>

    <form action="/rsvp/<%= attendee.token %>" method="POST" class="rsvp-form">
      <label for="name_disabled"> <!-- Added for attribute for accessibility -->
        Your Name:
        <input type="text" id="name_disabled" name="name_display" value="<%= attendee.name %>" disabled /> <!-- Changed name to name_display to avoid submission -->
      </label>
      <fieldset>
        <legend>Will you attend?</legend>
        <label><input type="radio" name="rsvp" value="yes" <% if (attendee.rsvp === 'yes') { %>checked<% } %> required /> Yes</label>
        <label><input type="radio" name="rsvp" value="no" <% if (attendee.rsvp === 'no') { %>checked<% } %> /> No</label>
      </fieldset>
      <label for="party_size_rsvp"> <!-- Added for attribute for accessibility -->
        Party Size:
        <input type="number" id="party_size_rsvp" name="party_size" min="1" value="<%= attendee.party_size %>" required />
      </label>
      <button type="submit">Submit RSVP</button>
    </form>
  </div>
</body>
</html>
